class LinkedList:

    # constructor
    def __init__ (self):
        self.head_node = None
        
        
    # add_to_front
    def AddToFront(self, newdata):
        new_node = Node(data)
        new_node.ref = self.head_node
        self.head_node
        
    print("exit with code 0")
